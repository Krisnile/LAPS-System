# 归档模板

本目录为历史/示例页面模板，主站不再使用。

- **访问方式**：仅通过 `/examples/<name>/` 渲染（如 `examples/tables.html` → `archived_templates/tables.html`）。
- **列表页**：`/examples/` 已重定向到首页，不再提供示例列表。
- **RTL / 其他**：`includes/navigationrtl.html`、`rtlsidebar.html` 为占位，部分链接可能失效。

如需恢复某页到主站，将对应模板移回 `templates/pages/` 并添加路由与视图即可。
